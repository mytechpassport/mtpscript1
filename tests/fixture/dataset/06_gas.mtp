function gas_intensive_computation(iterations: number) {
    if (iterations == 0) { 0 } else { gas_intensive_computation(iterations - 1) + 1 }
}

api GET "/gas" uses {} {
  const result = gas_intensive_computation(1500);
  respond json({
      "computation": result,
      "gas_features": {
          "metering": "enabled",
          "limits": "enforced",
          "exhaustion": "deterministic_error"
      }
  })
}
