api GET /test-gas
uses {} {
  function loop(n: number): number {
    if n == 0 { 0 } else { loop(n - 1) + 1 }
  }
  const small = loop(10)  // ok
  const large = loop(100000)  // exhaust
  respond json({ small })
}