function double(x: number) { x * 2 }

function add_one(x: number) { x + 1 }

api GET "/test-pipeline" uses {} {
  const result = add_one(double(5));
  const result2 = double(add_one(10));
  respond json({ "result": result, "result2": result2 })
}