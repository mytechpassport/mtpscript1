api GET /test-pipeline
uses {} {
  function double(x: number): number { x * 2 }
  function add_one(x: number): number { x + 1 }
  const result = 5 |> double |> add_one
  const result2 = 10 |> add_one |> double
  respond json({ result, result2 })
}